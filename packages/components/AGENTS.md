# AGENTS.md

> Auto-generated by [agentsmith](https://github.com/jpoindexter/agentsmith)

## TL;DR

- **Stack**: TypeScript 5.3.3 + Tailwind 4.0.0 + shadcn/ui
- **Components**: 0 total — USE EXISTING, don't create new
- **Key imports**: `cn()` from `@/lib/utils`

**Rules**: Use design tokens (not hardcoded colors), use `cn()` for classes, check existing components first

## Getting Started

```bash
npm install

# Start development
npm run dev
```

## Project Overview

| | |
|---|---|
| **Framework** | Unknown |
| **Language** | TypeScript |
| **Styling** | Tailwind CSS |
| **UI Library** | shadcn/ui (18 Radix packages) |
| **Class Utility** | `cn()` from `@/lib/utils` |
| **Components** | 0 |
| **Patterns** | 2 detected |
| **Codebase** | 122 files, 17,223 lines |

## Critical Rules

**These rules are NON-NEGOTIABLE:**

### 1. USE EXISTING COMPONENTS
This project has 0 components. Check the list below before creating anything new.

```tsx
// WRONG
<div className="rounded border p-4">...</div>

// RIGHT
<Card><CardContent>...</CardContent></Card>
```

### 2. USE DESIGN TOKENS
Never hardcode colors. Use semantic tokens that work with theme switching.

```tsx
// WRONG
className="bg-blue-500 text-white"
style={{ color: "#3b82f6" }}

// RIGHT
className="bg-primary text-primary-foreground"
```

### 3. USE `cn()` FOR CLASSES
Import from `@/lib/utils`. Handles conditional classes correctly.

```tsx
// WRONG
className={"btn " + (active ? "btn-active" : "")}

// RIGHT
className={cn("btn", active && "btn-active")}
```

### Framework-Specific

- Never use arbitrary values (`w-[137px]`) — use scale values
- Use `asChild` prop when wrapping components with custom elements
- Use CVA variants for consistent component styling
- Type all props and function parameters
- Avoid `any` — use proper types or `unknown`

## Component Props Types

- **AuditLogProps** — `src/admin/audit-log.tsx`
  - className: string, onExport: () => Promise<void>, onRefresh: () => Promise<void>, initialLogs: Audit...
- **AdminMetricsCardProps** — `src/admin/metrics-card.tsx`
  - code: string, title: string, value: string, change: number, changeLabel: string, icon: React.ReactNo...
- **SystemHealthWidgetProps** — `src/admin/system-health.tsx`
  - code: string, uptime: number, avgResponseTime: number, errorRate: number, requestsPerMinute: number,...
- **AiChatProps** — `src/ai/chat.tsx`
  - initialMessages: Message[], models: Model[], defaultModelId: string, conversations: Conversation[], ...
- **AreaChartProps** — `src/charts/area-chart.tsx`
  - data: AreaChartDataPoint[], xAxisKey: string, series: AreaChartSeries[], height: number, showGrid: b...
- **AreaChartCardProps** — `src/charts/area-chart.tsx`
  - title: string, description: string, code: string, icon: React.ReactNode, headerActions: React.ReactN...
- **StackedAreaChartProps** — `src/charts/area-chart.tsx`
  - stackKeys: string[], stackLabels: string[], stackColors: string[]
- **BarChartProps** — `src/charts/bar-chart.tsx`
  - data: BarChartDataPoint[], xAxisKey: string, series: BarChartSeries[], height: number, showGrid: boo...
- **BarChartCardProps** — `src/charts/bar-chart.tsx`
  - title: string, description: string, code: string, icon: React.ReactNode, headerActions: React.ReactN...
- **StackedBarChartProps** — `src/charts/bar-chart.tsx`
  - stackKeys: string[], stackLabels: string[], stackColors: string[]
- **LineChartProps** — `src/charts/line-chart.tsx`
  - data: LineChartDataPoint[], xAxisKey: string, series: LineChartSeries[], height: number, showGrid: b...
- **LineChartCardProps** — `src/charts/line-chart.tsx`
  - title: string, description: string, code: string, icon: React.ReactNode, headerActions: React.ReactN...
- **NotificationCenterProps** — `src/notifications/notification-center.tsx`
  - notifications: NotificationCenterItem[], onMarkAsRead: (id: string) => void, onMarkAllAsRead: () => ...
- **BackupCodesModalProps** — `src/security/backup-codes-modal.tsx`
  - open: boolean, onOpenChange: (open: boolean) => void, codes: string[], onRegenerate: () => Promise<s...
- **MfaCardProps** — `src/security/mfa-card.tsx`
  - twoFactorEnabled: boolean, isEnabling2FA: boolean, isDisabling2FA: boolean, onEnable2FA: () => void,...

## Model Types

- **Model** — id: string, name: string, provider: string, maxTokens: number
- **SchemaScriptProps** — schema: object, nonce: string

## Utilities

- `cn(...classes)` — Class merging utility from `@/lib/utils`

## Key Dependencies

Most used external packages:

- `@fabrk/design-system` — 96 imports
- `react` — 95 imports
- `lucide-react` — 32 imports
- `class-variance-authority` — 7 imports
- `@radix-ui/react-slot` — 5 imports
- `@tanstack/react-table` — 4 imports
- `@radix-ui/react-dialog` — 3 imports
- `date-fns` — 2 imports
- `clsx` — 1 imports
- `tailwind-merge` — 1 imports
- `@radix-ui/react-accordion` — 1 imports
- `@radix-ui/react-alert-dialog` — 1 imports
- `@radix-ui/react-avatar` — 1 imports
- `@radix-ui/react-checkbox` — 1 imports
- `prism-react-renderer` — 1 imports

## Code Patterns

Detected patterns in this codebase:

- Forms: react-hook-form
- Components: Radix Slot pattern (asChild)

## Additional Guidelines

**Unvalidated API inputs**
- Wrong: `const { id } = req.body; // No validation`
- Right: `const { id } = schema.parse(req.body);`

## Commands

```bash
npm run dev          # tsup --watch
npm run build        # tsup
npm test             # vitest run
npm run lint         # eslint src
npm run typecheck    # tsc --noEmit
```
