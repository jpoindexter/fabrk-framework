# AGENTS.md

> Auto-generated by [hashmark](https://hashmark.md)

## TL;DR

- **Stack**: TypeScript 5.3.3
- **Components**: 2 total — USE EXISTING, don't create new
- **Key imports**: `cn()` from `@/utils`

**Rules**: Use design tokens (not hardcoded colors), use `cn()` for classes, check existing components first

## Getting Started

```bash
npm install

# Start development
npm run dev
```

## Project Overview

| | |
|---|---|
| **Framework** | Unknown |
| **Language** | TypeScript |
| **Class Utility** | `cn()` from `@/utils` |
| **Components** | 2 |
| **Codebase** | 38 files, 5,012 lines |

## Critical Rules

**These rules are NON-NEGOTIABLE:**

### 1. USE EXISTING COMPONENTS
This project has 2 components. Check the list below before creating anything new.

```tsx
// WRONG
<div className="rounded border p-4">...</div>

// RIGHT
<Card><CardContent>...</CardContent></Card>
```

### 2. USE DESIGN TOKENS
Never hardcode colors. Use semantic tokens that work with theme switching.

```tsx
// WRONG
className="bg-blue-500 text-white"
style={{ color: "#3b82f6" }}

// RIGHT
className="bg-primary text-primary-foreground"
```

### 3. USE `cn()` FOR CLASSES
Import from `@/utils`. Handles conditional classes correctly.

```tsx
// WRONG
className={"btn " + (active ? "btn-active" : "")}

// RIGHT
className={cn("btn", active && "btn-active")}
```

### Framework-Specific

- Type all props and function parameters
- Avoid `any` — use proper types or `unknown`

## Components

2 components across 1 categories.

### Src (2)

- `FabrkContextProvider` — `@/context`
  - Props: config, registry, features
  - FABRK Framework Context
- `FabrkProvider`, `RootLayout` — `@/providers`
  - Props: config, registry, features
  - Provider components for FABRK framework

## Component Props Types

- **FabrkContextProviderProps** — `src/context.tsx`
  - children: ReactNode, config: FabrkConfig, registry: PluginRegistry, features: FeatureModules
- **FabrkProviderProps** — `src/providers.tsx`
  - children: ReactNode, config: FabrkConfig, registry: PluginRegistry, features: FeatureModules

## Utilities

- `cn(...classes)` — Class merging utility from `@/utils`

## Key Dependencies

Most used external packages:

- `react` — 2 imports
- `@fabrk/config` — 1 imports
- `clsx` — 1 imports
- `tailwind-merge` — 1 imports

## Additional Guidelines

**Unvalidated API inputs**
- Wrong: `const { id } = req.body; // No validation`
- Right: `const { id } = schema.parse(req.body);`

## Commands

```bash
npm run dev          # tsup --watch
npm run build        # tsup
npm run lint         # eslint src
npm run typecheck    # tsc --noEmit
```
