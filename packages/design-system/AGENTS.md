# AGENTS.md

> Auto-generated by [hashmark](https://hashmark.md)

## TL;DR

- **Stack**: TypeScript 5.3.3
- **Components**: 1 total — USE EXISTING, don't create new

**Rules**: Use design tokens (not hardcoded colors), use `cn()` for classes, check existing components first

## Getting Started

```bash
npm install

# Start development
npm run dev
```

## Project Overview

| | |
|---|---|
| **Framework** | Unknown |
| **Language** | TypeScript |
| **Components** | 1 |
| **Codebase** | 21 files, 4,035 lines |

## Critical Rules

**These rules are NON-NEGOTIABLE:**

### 1. USE EXISTING COMPONENTS
This project has 1 components. Check the list below before creating anything new.

```tsx
// WRONG
<div className="rounded border p-4">...</div>

// RIGHT
<Card><CardContent>...</CardContent></Card>
```

### 2. USE DESIGN TOKENS
Never hardcode colors. Use semantic tokens that work with theme switching.

```tsx
// WRONG
className="bg-blue-500 text-white"
style={{ color: "#3b82f6" }}

// RIGHT
className="bg-primary text-primary-foreground"
```

### Framework-Specific

- Type all props and function parameters
- Avoid `any` — use proper types or `unknown`

## Components

1 components across 1 categories.

### Providers (1)

- `ThemeProvider`, `ThemeScript` — `@/providers/ThemeProvider`
  - Props: defaultTheme, defaultColorTheme, storageKey, storageKeyPrefix, persist
  - Inline script to prevent flash of unstyled content

## Component Props Types

- **ThemeProviderProps** — `src/providers/ThemeProvider.tsx`
  - children: React.ReactNode, defaultTheme: ThemeName, defaultColorTheme: ColorThemeName, storageKey: s...
- **ThemeScriptProps** — `src/providers/ThemeProvider.tsx`
  - storageKey: string, storageKeyPrefix: string, defaultColorTheme: ColorThemeName, nonce: string


## Additional Guidelines

**Unvalidated API inputs**
- Wrong: `const { id } = req.body; // No validation`
- Right: `const { id } = schema.parse(req.body);`

## Commands

```bash
npm run dev          # tsup --watch
npm run build        # tsup
npm run lint         # eslint src
npm run typecheck    # tsc --noEmit
```
